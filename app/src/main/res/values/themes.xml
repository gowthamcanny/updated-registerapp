<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RegisterUserApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RegisterUserApplication" parent="Base.Theme.RegisterUserApplication" />

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextStyle">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/josefinsans_bold</item>
        <item name="android:lineSpacingExtra">0.5dp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingEnd">4dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/cardview_light_background</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="custom_text_input" parent="AppTheme">
        <item name="colorAccent">#99979BA2</item>
        <item name="android:textColorHint">#99979BA2</item>
    </style>
</resources>